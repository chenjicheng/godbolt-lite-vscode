Open a `.c`, `.cc`, `.cpp`, `.h`, or `.hpp` file and run `Godbolt Lite: Open Assembly`.

Godbolt Lite opens a read-only `godbolt-lite:` document beside the source file. It compiles dirty editors through a temporary file, so the assembly follows your current editor text.
